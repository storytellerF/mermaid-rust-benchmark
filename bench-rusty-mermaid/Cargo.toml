[package]
name = "bench-rusty-mermaid"
version = "0.1.0"
edition = "2021"

[dependencies]
rusty-mermaid = { version = "0.2", features = ["svg"] }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
