[package]
name = "bench-mermaid-rs-renderer"
version = "0.1.0"
edition = "2021"

[dependencies]
mermaid-rs-renderer = "0.3"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
